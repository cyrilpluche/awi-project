import { createMuiTheme } from '@material-ui/core/styles';

const  classic = createMuiTheme({
    palette: {
        secondary: {
            main: '#ffc50a'
        }
    },
});

export const Theme = {
    classic
}