import { memberAction} from "../actions/Member.action";

const labels = memberAction.labels

const initialState = {

};

export function member (state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
};
